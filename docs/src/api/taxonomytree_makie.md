```@meta
CurrentModule = PaleobiologyDB.TaxonomyMakie
```

# TaxonomyMakie — API Reference

`PaleobiologyDB.TaxonomyMakie` is a package extension that renders
[`TaxonomyTree`](@ref) objects as Makie dendrograms and provides the PBDB–PhyloPic
bridge.  It activates automatically when both a Makie backend (e.g. `CairoMakie`)
and `PhyloPicMakie` are loaded. Bring its exports into scope with
`using PaleobiologyDB.TaxonomyMakie` or call them through the
`PaleobiologyDB.TaxonomyMakie` submodule explicitly.

See the [TaxonomyMakie guide](../guide/taxonomytree_makie.md) for installation
instructions, worked examples, and a full attribute reference.

```@docs
TaxonomyMakie
```

## Plot type

`TaxonomyTreePlot = Plot{taxonomytreeplot}` is the Makie plot type generated by the
`@recipe` macro. For the full attribute table and usage examples, see the
[TaxonomyMakie guide](../guide/taxonomytree_makie.md).

## Standalone figure

```@docs
taxonomytreeplot
```

## Existing axis

`taxonomytreeplot!(ax, tree; kwargs...)` — same keyword arguments as [`taxonomytreeplot`](@ref).
See the [TaxonomyMakie guide](../guide/taxonomytree_makie.md) for usage examples.

## Axis helpers

```@docs
set_rank_axis_ticks!
```

## PhyloPic augmentation

```@docs
augment_leaf_phylopic!
leaf_positions
```
